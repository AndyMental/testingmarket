<div class="nav-container navbar-fixed-top" data-component="Navbar">
  <!-- /.sub-navbar -->
  <nav class="navbar navbar-default">
    <div class="container">
      <div class="navbar-header" style="font-size: 30px;padding-top: 10px;">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to root_path do %>
        Kaira
        <% end %>
      </div>
      <div class="collapse navbar-collapse" id="myNavbar">
        <ul class="nav navbar-nav z-nav">
          <li class="active">
            <%= link_to 'Home', root_path %>

          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Products
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu multi-column columns-3">
              <div class="row">
                <% unless @productsegment.nil? %>
                <% @productsegment.limit(3).each do |productsegment| %>
                <div class="col-sm-4">
                  <ul class="multi-column-dropdown">
                    <h3>
                      <%=link_to "#{productsegment.title}", products_path(product_segment_id: productsegment.id) %>
                    </h3>
                    <% unless @categories.nil? %>
                    <% @categories.where(product_segment_id: productsegment.id).each do |category| %>
                    <li>
                      <%=link_to "#{category.title}", products_path(category_id: category.id) %>
                    </li>
                    <% end %>
                    <% end %>
                  </ul>
                </div>
                <% end %>
                <% end %>
              </ul>
            </li>
            <li>
              <%= link_to "Catalogues", products_path  %>
            </li>
            <li class="<%= active_class('/about') %>">
              <%= link_to 'About', about_path %>

            </li>
            <li class="<%= active_class('/contact') %>">
             <%= link_to 'Contact', contact_path %>

           </li>
         </ul>
         <ul class="nav navbar-nav navbar-right" style="display: -webkit-inline-box;">
          <li>
            <%= link_to "" do %>
            <span class="cart_item_number">
              2
            </span>
            <%= fa_icon "shopping-cart" %>
            <% end %>
          </li>
          <li>
           <%= link_to "" do %>
           <%= fa_icon "user" %>
           <% end %>
         </li>
       </ul>
     </div>
   </div>
 </nav>
</div>
